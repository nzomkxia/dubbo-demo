<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:dubbo="http://dubbo.apache.org/schema/dubbo"
       xmlns="http://www.springframework.org/schema/beans"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
       http://dubbo.apache.org/schema/dubbo http://dubbo.apache.org/schema/dubbo/dubbo.xsd">

    <!-- consumer's application name, used for tracing dependency relationship (not a matching criterion),
    don't set it same as provider -->
    <dubbo:application name="meetup-demo-consumer"/>

    <!-- use multicast registry center to discover service -->
    <dubbo:configcenter address="zookeeper://127.0.0.1:2181"/>

    <!-- generate proxy for the remote service, then demoService can be used in the same way as the
    local regular interface -->
    <dubbo:reference id="demoService" check="false" interface="org.apache.dubbo.demo.api.DemoService"/>
    <dubbo:reference id="userService" check="false" interface="org.apache.dubbo.demo.api.UserService"/>
    <dubbo:reference id="timeoutTestService" check="false" interface="org.apache.dubbo.demo.api.TimeoutTestService"/>
    <dubbo:reference id="noResponseService" check="false" interface="org.apache.dubbo.demo.api.NoResponseService"/>

</beans>
